# Dependencies
node_modules/

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.env
*.egg-info/
dist/
build/
.eggs/
.pytest_cache/
.cache/
.coverage
coverage.xml
htmlcov/
.mypy_cache/

# Pre-commit
.pre-commit-cache/

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/*
!.vscode/settings.json
.idea/
*.swp
*.swo

# Lock files (optional - uncomment to exclude)
# package-lock.json

# Documentation build briefs (committed; not Python build output)
!docs/build/

# Local-only agent working paths. These are never committed. The Markdown lint
# scripts skip them by name, so tests/test_lint_exclusions.py makes sure that
# these paths and those lint exclusions stay in agreement.
.orchestration/
ORCHESTRATOR_PROMPT.md
